using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TextControl : MonoBehaviour
{
    /*
    public Text TextUpdate;
    string Text_System;
    int tan_coe_local;
    int tan_lev_local;
    //public GameObject TanContentUpdate;
    // Start is called before the first frame update
    void Start()
    {
        Text_System = "<color=red><size=16>" + "Tantrum System\nStart" + "</size></color>";
        tan_coe_local = -1;
        tan_lev_local = -1;
    }

    // Update is called once per frame
    void Update()
    {
        if (!(tan_coe_local == GazeControl.tan_coe) || !(tan_lev_local == Action_System.tan_lev))
        {
            tan_coe_local = GazeControl.tan_coe;
            tan_lev_local = Action_System.tan_lev;
        }
        UpdateText();
    }

    void UpdateText()
    {
        Text_System = "<color=red><size=16>" + "Tantrum System\n" + "</size></color>" +
            "<color=blue><size=8>" + "Tantrum Coefficient = " + "</size></color>" +
            "<color=red><size=12>" + tan_coe_local.ToString() + "\n" + "</size></color>" +
            "<color=blue><size=8>" + "Tantrum Coefficient = " + "</size></color>" +
            "<color=red><size=12>" + tan_lev_local.ToString() + "</size></color>";
        TextUpdate.text = Text_System;
    }
    */
}
